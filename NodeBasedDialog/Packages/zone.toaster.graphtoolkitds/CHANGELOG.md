# Changelog

## 1.0.0 - 2026-06-04

- Converted the asset from an `Assets/Plugins` install layout to a Unity Package Manager package.
- Added Git URL installation support through the package subfolder path.
- Moved runtime scripts, prefabs, resources, and sprites into `Runtime`.
- Moved editor tooling, Graph Toolkit authoring, compiler, and migration utilities into `Editor`.
- Moved the demo scene and related assets into `Samples~/Demo`.
- Moved the original PDF guide into `Documentation~`.
- Declared Unity package dependencies in `package.json`.
